# Default for tests: no Kafka broker and no Redis. KafkaOrderFlowIT / PortfolioCacheIT turn these back on.
app.outbox.publisher.enabled=false
spring.kafka.listener.auto-startup=false
spring.kafka.admin.auto-create=false
app.cache.portfolio.enabled=false
